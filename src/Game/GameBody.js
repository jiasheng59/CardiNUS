/*
import React from "react";

const phases = [
    "Choose attires",
    "Night",
    "Discussion",
    "Vote a Captain",
    "Captain time",
    "Vote for Alien",
    "Astronauts win",
    "Alien and Mr. D win"
]

function Description(props) {
    if (props.phase === "Choose attire") {
        return (

        );
    }
    if (props.phase === "Night") {
        return (
            <div>
                You may swap an attire with any other player.
            </div>
        );
    }
    if (props.phase === "Discussion") {
        return (
            <div>
                Discussion time left: <Timer secondsLeft={300}></Timer>
            </div>
        );
    }
    if (props.phase === "Vote for Alien") {
        return (

        );
    }
}

class GameBody extends React.Component {
    render() {
        const phase;
        return <Description phase={}></Description>
    }
}

*/